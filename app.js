const $=s=>document.querySelector(s),fmt=n=>Number(n||0).toLocaleString('pt-BR');let catalog,locais,currentCandidate=null,currentDetail=null,currentMunicipio=null,chunkCache={};const cargoSel=$('#cargo'),candSel=$('#candidato'),busca=$('#busca'),conteudo=$('#conteudo'),resumo=$('#resumo'),voltar=$('#voltar');async function init(){[catalog,locais]=await Promise.all([fetch('dados/2022/catalogo.json').then(r=>r.json()),fetch('dados/2022/locais.json').then(r=>r.json())]);cargoSel.innerHTML=catalog.cargos.map(c=>`<option value="${c.codigo}">${c.nome}</option>`).join('');cargoSel.value=catalog.cargos.find(c=>c.nome==='Deputado Estadual')?.codigo||catalog.cargos[0].codigo;refreshCandidates();renderCandidates()}function cargo(){return catalog.cargos.find(c=>String(c.codigo)===String(cargoSel.value))}function refreshCandidates(){const c=cargo();candSel.innerHTML='<option value="">Todos os candidatos</option>'+c.candidatos.map(x=>`<option value="${x.id}">${x.nome} · ${x.partido}</option>`).join('');currentCandidate=null;currentDetail=null;currentMunicipio=null;busca.placeholder='Buscar candidato...';voltar.classList.add('hidden');resumo.innerHTML=''}function renderCandidates(){const q=busca.value.trim().toLocaleUpperCase('pt-BR'),arr=cargo().candidatos.filter(c=>!q||(`${c.nome} ${c.numero} ${c.partido}`).toLocaleUpperCase('pt-BR').includes(q));conteudo.innerHTML=`<div class="table-head"><div>#</div><div>Candidato</div><div style="text-align:right">Votos</div></div>`+arr.map((c,i)=>`<div class="row" data-cid="${c.id}"><div class="rank">${i+1}</div><div><div class="name">${c.nome}</div><div class="sub">${c.numero} · ${c.partido}</div></div><div class="votes">${fmt(c.total)}</div></div>`).join('');conteudo.querySelectorAll('[data-cid]').forEach(el=>el.onclick=()=>{candSel.value=el.dataset.cid;loadCandidate(el.dataset.cid)})}async function loadCandidate(cid){const meta=cargo().candidatos.find(c=>c.id===cid);if(!meta)return;currentCandidate=meta;currentMunicipio=null;busca.value='';busca.placeholder='Buscar município...';voltar.classList.add('hidden');let d=chunkCache[meta.arquivo];if(!d){conteudo.innerHTML='<div class="loading">Carregando votação…</div>';d=await fetch('dados/2022/'+meta.arquivo).then(r=>r.json());chunkCache[meta.arquivo]=d}currentDetail=d[cid];renderMunicipios()}function renderMunicipios(){const q=busca.value.trim().toLocaleUpperCase('pt-BR'),arr=currentDetail.m.filter(x=>!q||x[1].toLocaleUpperCase('pt-BR').includes(q));resumo.innerHTML=`<div class="kpi"><small>CANDIDATO</small><strong>${currentCandidate.nome}</strong><div class="sub">${currentCandidate.numero} · ${currentCandidate.partido}</div></div><div class="kpi"><small>TOTAL MT</small><strong>${fmt(currentCandidate.total)}</strong></div><div class="kpi"><small>MUNICÍPIOS COM VOTOS</small><strong>${fmt(currentDetail.m.length)}</strong></div>`;conteudo.innerHTML=`<div class="table-head"><div>#</div><div>Município</div><div style="text-align:right">Votos</div></div>`+arr.map((m,i)=>`<div class="row" data-mid="${m[0]}"><div class="rank">${i+1}</div><div class="name">${m[1]}</div><div class="votes">${fmt(m[2])}</div></div>`).join('');conteudo.querySelectorAll('[data-mid]').forEach(el=>el.onclick=()=>renderMunicipio(el.dataset.mid))}function renderMunicipio(mid){const loc=locais[String(mid)],m=currentDetail.m.find(x=>String(x[0])===String(mid));if(!loc||!m)return;currentMunicipio=mid;voltar.classList.remove('hidden');busca.value='';busca.placeholder='Filtrar bairro ou local...';const voteMap=new Map(currentDetail.s.filter(x=>String(x[0])===String(mid)).map(x=>[`${x[1]}|${x[2]}`,x[3]])),bairros={};for(const s of loc.secoes){const [zona,secao,localCode,bairro,localName]=s,v=voteMap.get(`${zona}|${secao}`)||0;if(!v)continue;const b=bairros[bairro]||(bairros[bairro]={total:0,locais:{}});b.total+=v;const lk=`${localCode}|${localName}`,l=b.locais[lk]||(b.locais[lk]={code:localCode,name:localName,total:0,secoes:[]});l.total+=v;l.secoes.push([zona,secao,v])}window._bairroData=Object.entries(bairros).sort((a,b)=>b[1].total-a[1].total||a[0].localeCompare(b[0],'pt-BR'));resumo.innerHTML=`<div class="kpi"><small>MUNICÍPIO</small><strong>${m[1]}</strong></div><div class="kpi"><small>VOTOS</small><strong>${fmt(m[2])}</strong><div class="sub">${currentCandidate.nome}</div></div><div class="kpi"><small>BAIRROS COM VOTOS</small><strong>${fmt(window._bairroData.length)}</strong></div>`;renderBairros()}function renderBairros(){const q=busca.value.trim().toLocaleUpperCase('pt-BR'),ba=(window._bairroData||[]).filter(([bn,b])=>!q||bn.toLocaleUpperCase('pt-BR').includes(q)||Object.values(b.locais).some(l=>l.name.toLocaleUpperCase('pt-BR').includes(q)));conteudo.innerHTML=`<div class="detail-title"><h2>Detalhamento territorial</h2><p>Bairro → local de votação → seção</p></div>`+(ba.length?ba.map(([bn,b],bi)=>`<div class="bairro"><button data-b="${bi}"><div><div class="bairro-title">${bn}</div><div class="count">${Object.keys(b.locais).length} local(is)</div></div><div><span class="v">${fmt(b.total)}</span><span class="arrow">▾</span></div></button><div class="nested hidden" id="b${bi}">${Object.values(b.locais).sort((a,z)=>z.total-a.total).map((l,li)=>`<div class="local"><button data-l="${bi}-${li}"><div><div class="name">${l.name}</div><div class="count">Local ${l.code} · ${l.secoes.length} seção(ões)</div></div><div><span class="v">${fmt(l.total)}</span><span class="arrow">▾</span></div></button><div class="sections hidden" id="l${bi}-${li}">${l.secoes.sort((a,z)=>a[1]-z[1]).map(s=>`<div class="section-row"><div>Zona ${s[0]} · Seção ${s[1]}</div><div class="v">${fmt(s[2])}</div></div>`).join('')}</div></div>`).join('')}</div></div>`).join(''):'<div class="empty">Nenhum resultado encontrado.</div>');conteudo.querySelectorAll('[data-b]').forEach(x=>x.onclick=()=>document.getElementById('b'+x.dataset.b).classList.toggle('hidden'));conteudo.querySelectorAll('[data-l]').forEach(x=>x.onclick=()=>document.getElementById('l'+x.dataset.l).classList.toggle('hidden'))}cargoSel.onchange=()=>{busca.value='';refreshCandidates();renderCandidates()};candSel.onchange=()=>{busca.value='';if(candSel.value)loadCandidate(candSel.value);else{refreshCandidates();renderCandidates()}};busca.oninput=()=>currentMunicipio?renderBairros():currentCandidate?renderMunicipios():renderCandidates();voltar.onclick=()=>{currentMunicipio=null;voltar.classList.add('hidden');busca.value='';busca.placeholder='Buscar município...';renderMunicipios()};init().catch(e=>{conteudo.innerHTML='<div class="empty">Não foi possível carregar os dados. Abra pelo GitHub Pages.</div>';console.error(e)});